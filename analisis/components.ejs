```{=html}
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Listado de Contenido</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@100;300;400;500&display=swap" rel="stylesheet">

<style>
  .wrap {
    margin: 20px auto;
    width: 90%;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 20px;
    max-width: 1500px;
    background-color: transparent; 
  }

  .tile {
    width: 263px;
    height: 780px;
    background-color: transparent;
    border-radius: 50%;
    position: relative;
    cursor: pointer;
    transition: all 0.4s ease-out;
    box-shadow: none;
    overflow: hidden;
    color: white;
    font-family: 'Roboto';
  }

  .tile img {
    height: 100%;
    width: 100%;
    position: absolute;
    top: 0;
    left: 0;
    z-index: 0;
    transition: all 0.4s ease-out;
    object-fit: cover;
    filter: none;
    mix-blend-mode: normal;
    border-radious: 50%
  }

  .tile .text {
    position: absolute;
    z-index: 1;
    padding: 30px;
    font-size: 20px;
    height: calc(100% - 60px);
    background: transparent;
  }

  .tile h1 {
    font-weight: 300;
    margin: 0;
    color: black;
    text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.3);
  }

  .tile p {
    font-weight: 300;
    margin-top: 20px;
    line-height: 25px;
    transform: translateX(-200px);
    transition: all 0.6s ease-in-out;
    opacity: 0;
    color: black
  }

.tile:hover {
  transform:scale(1.05);
  border-style: solid;
  border-color: #ECA0A0;
  opacity: 1;
}

  .tile:hover img {
    transform:scale(1.05);
    opacity: 1;
    transform: translateX(0);
  }

  .tile:hover p {
    transform: translateX(0);
    opacity: 1;
  }

  @media (max-width: 768px) {
    .tile {
      width: 90%;
      height: 400px;
    }
  }
</style>

</head>
<body>

  <div class="wrap">
    <% for (const item of items) { %>
      <div class="tile">
        <a href="<%= item.path %>" role="button" style="text-decoration: none; color: inherit; background: transparent; border: none; display: block; height: 100%; width: 100%;">
          <img alt="<%= item.title %>" src="<%= item.image %>">

          <div class="text">
            <p><%= item.title%></p>
            
          </div>
        </a>
      </div>
    <% } %>
  </div>

</body>
</html>
```
